// Personalize the page here.
//
// Files live in the `public/` folder and are served from the site root:
//      public/song.mp3     → the background song (currently your music.mp3)
//      public/manish.jpg   → the birthday photo
//
// To change the song later, replace public/song.mp3 (any name works if you
// also update MUSIC_SRC below). The photo falls back to a gold monogram until
// manish.jpg exists.

export const PHOTO_SRC = '/manish.jpg'

// Background song.
export const MUSIC_SRC = '/song.mp3'

// Optional backup source if MUSIC_SRC is missing. Empty = none.
export const MUSIC_FALLBACK_SRC = ''
